//! Inference benchmark scaffold; it intentionally requires an explicitly
//! configured local model instead of inventing synthetic token latency.
fn main() {
    println!("inference benchmark scaffold: measure 256 generated tokens on the target model");
}
