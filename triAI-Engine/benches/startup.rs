//! Startup microbenchmark scaffold. Record results externally in the JSON
//! schema consumed by `tri-quality-gates`; no model is started implicitly.
fn main() {
    println!("startup benchmark scaffold: run against a configured air-gapped engine");
}
